CMAKE_MINIMUM_REQUIRED(VERSION 3.0 FATAL_ERROR)

PROJECT(CMake-Tools LANGUAGES)

ENABLE_TESTING()

FILE(
  GLOB FILES
  LIST_DIRECTORIES true
  "${CMAKE_SOURCE_DIR}/modules/*.cmake")

FOREACH(FILE IN LISTS FILES)
  INCLUDE("${FILE}")
ENDFOREACH()
